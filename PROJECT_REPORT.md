# تقرير مشروع ICS — نظام قيادة الاستثمار

| | |
|---|---|
| **المشروع** | ICS — Investment Command System |
| **الإصدار** | MVP v1.0 |
| **النطاق** | السوق الأمريكي — تداول ورقي + تقارير تيليجرام |
| **الوضع** | محلّل/متدرّب (Analyst → Trainee) |
| **التاريخ** | 26 يونيو 2026 |
| **الحالة** | 🟢 منشور ويعمل 24/7 على Render |
| **المستودع** | github.com/Jabbar997/ics-bot |

> ⚠️ **تنبيه:** النظام **محاكاة تعليمية وتداول ورقي فقط**. لا يتصل بأي وسيط حقيقي،
> لا ينفّذ صفقات حقيقية، ولا يقدّم نصيحة مالية ولا يضمن ربحًا.

---

## 1. نبذة عن المشروع
ICS ليس «بوت تداول» بسيطًا، بل **محرّك قرار ومراجعة** متكامل. كل قرار محتمل يمرّ
بخط أنابيب ثابت: بيانات السوق ← تنظيف ← مؤشرات ← تحليل حالة السوق ← استراتيجيات ←
درجة جودة القرار (DQS) ← مدير المخاطر ← وسيط ورقي ← سجل تدقيق ← تقييم أداء ← تقارير
تيليجرام. رأس المال الافتراضي **$266**، والمقارنة المرجعية **SPY**.

---

## 2. ما الذي أنجزناه (الرحلة الكاملة)

| المرحلة | الإنجاز |
|---|---|
| ① بناء الـ MVP | حزمة بايثون معيارية كاملة (≈ 69 ملفًا) بكل طبقات المعمارية |
| ② الاختبارات | 50 اختبار pytest — جميعها تمر ✅ |
| ③ الباك تيست | تشغيل على **5 سنوات بيانات حقيقية** من yfinance + تقسيم Walk-forward |
| ④ التعريب | تعريب كامل لواجهة البوت (الأوامر، التقارير، الأسباب، الرسائل) |
| ⑤ النشر | نشر دائم على **Render** (Background Worker) مع قرص دائم |
| ⑥ الجدولة | دمج التقارير التلقائية (يومي/أسبوعي) داخل عملية البوت نفسها |

---

## 3. المعمارية (خط الأنابيب)
```
طبقة بيانات السوق (yfinance — قراءة فقط)
      ↓ تنظيف وتحقق (إزالة التكرار، الترتيب، رفض الناقص)
المؤشرات الفنية (MA/RSI/MACD/ATR/التذبذب/بيتا — يدوية وشفافة)
      ↓
محلّل حالة السوق (SPY → صاعد/صاعد ضعيف/عرضي/هابط/ذعر)
      ↓
محرك الاستراتيجيات (اتجاه، زخم، ارتداد، نقد دفاعي)
      ↓
درجة جودة القرار DQS (0–100، 5 مكوّنات، حد أدنى 70)
      ↓
مدير المخاطر (الحجم، الوقف، الحدود، قائمة المتابعة، مفتاح الإيقاف)
      ↓
محرك التداول الورقي (أوامر افتراضية فقط — لا وسيط حقيقي)
      ↓
سجل التدقيق (لكل قرار — قرار بلا سجل = غير صالح)
      ↓
مقيّم الأداء (عائد، شارب، تراجع، نسبة فوز، توقّع)
      ↓
بوت تيليجرام (تقارير + أوامر، محمي بالصلاحيات)
```

---

## 4. الوحدات المنفذة

| الطبقة | الملفات | الوظيفة |
|---|---|---|
| الإعداد | `config.py`, `config.yaml`, `.env` | قواعد العمل + الأسرار (pydantic) |
| البيانات | `data/market_data.py`, `cleaner.py` | جلب yfinance + تنظيف وتحقق |
| المؤشرات | `features/indicators.py`, `feature_engine.py` | MA20/50/200، RSI14، MACD، ATR14، الحجم، التذبذب، بيتا |
| حالة السوق | `market/regime.py` | تصنيف SPY إلى 5 حالات (الذعر يتقدّم على الكل) |
| الاستراتيجيات | `strategies/*` | اتجاه، زخم، ارتداد، نقد دفاعي + محرك اختيار |
| القرار | `decision/dqs.py`, `decision_engine.py` | DQS + تنسيق القرار والتنفيذ والتسجيل |
| المخاطر | `risk/risk_manager.py`, `kill_switch.py` | الحدود والحجم والوقف + مفتاح إيقاف 3 مستويات |
| التداول الورقي | `paper/broker.py`, `portfolio.py`, `orders.py` | وسيط افتراضي، محفظة، أوامر |
| الأداء | `performance/evaluator.py`, `benchmarks.py` | المقاييس + المقارنة بـ SPY |
| تيليجرام | `telegram/bot.py`, `commands.py`, `reports.py` | 14 أمرًا + تقارير + جدولة |
| قاعدة البيانات | `db/models.py`, `repositories.py`, `database.py` | 13 نموذجًا + طبقة مستودعات |
| الباك تيست | `backtesting/backtester.py`, `walk_forward.py` | محاكاة تاريخية بلا نظرة مستقبلية |

---

## 5. قاعدة البيانات والتدقيق
13 نموذج SQLAlchemy: المستخدم، رموز المتابعة، الأسعار، لقطات المؤشرات، حالات السوق،
**القرارات**، الأوامر الورقية، المراكز، لقطات المحفظة، **سجل التدقيق**، تقارير الأداء،
أحداث مفتاح الإيقاف، إعدادات النظام.

**القاعدة الذهبية:** كل قرار يُكتب مع سجل تدقيق في **معاملة واحدة** — لا يوجد قرار بلا
سجل. (تم التحقق منها: في الباك تيست عدد القرارات = عدد سجلات التدقيق تمامًا).

---

## 6. منطق القرار

**درجة جودة القرار (DQS) — من 100:**
| المكوّن | الوزن |
|---|---|
| توافق الاستراتيجية | 25 |
| إدارة المخاطر | 25 |
| جودة التوقيت | 20 |
| قوة حالة السوق | 15 |
| وضوح السبب | 15 |

أقل من 70 → رفض وتسجيل كـ«فرصة مرفوضة». الهدف متوسط ≥ 75.

**مدير المخاطر:** أقصى 3 مراكز، أقصى 10% للمركز، وقف = `min(2×ATR، 7%)`،
حدود خسارة (أسبوعي −5%، شهري −12%، تراجع −15%)، منع الرموز خارج القائمة، منع
الرافعة/البيع المكشوف/الخيارات.

**مفتاح الإيقاف:** 3 مستويات تلقائية (تحذير/تجميد/إيقاف كامل) + STOP يدوي فوري.

---

## 7. نتائج الباك تيست الحقيقي (5 سنوات)
**الفترة:** 2021-06-28 → 2026-06-25 · بيانات yfinance حقيقية لـ 20 رمزًا + SPY.

| المقياس | ICS | SPY |
|---|---|---|
| العائد الإجمالي | **+21.88%** | **+71.78%** |
| أقصى تراجع | **−5.42%** | ~ −25% (2022) |
| Sharpe | 1.01 | — |
| نسبة الفوز | 38.7% | — |
| متوسط DQS | 85.15 | — |

- **القرارات:** 10,568 · **شراء:** 166 · **فرص مرفوضة:** 10,239
- **صفقات مُغلقة:** 163 (تجاوزت حد التأهيل ≥100 ✅) · **سجلات تدقيق:** 10,568
- **القراءة:** النظام تحفّظي بطبيعته (حد أقصى 30% توظيف + نقد دفاعي عند ضعف SPY)،
  فالتقط ~30% من حركة SPY لكن بتراجع أقل بخمسة أضعاف — أي **حفظ رأس مال** متوافق مع
  سياسة المقارنة في المواصفات. لا نظرة مستقبلية (التنفيذ بسعر افتتاح اليوم التالي).

---

## 8. الاختبارات والجودة
**50 اختبار pytest — كلها تمر:** المؤشرات، حالة السوق، DQS، حدود المخاطر، حساب الحجم،
حساب الوقف، مفتاح الإيقاف، تنفيذ الأوامر الورقية، إنشاء سجل التدقيق، تسجيل الفرص
المرفوضة، **عدم وجود نظرة مستقبلية في الباك تيست**.

---

## 9. التعريب
واجهة البوت **عربية بالكامل**: كل ردود الأوامر، التقريران اليومي/الأسبوعي، أسباب
الإشارات (شراء/رفض/خروج)، رسائل الصلاحيات والأخطاء، وقائمة Menu في تيليجرام.
أسماء الأوامر (`/status`…) والرموز (`SPY`) والأرقام تبقى لاتينية. (رموز المخالفات
الداخلية بقيت إنجليزية لأن الاختبارات تعتمد عليها وهي غير ظاهرة للمستخدم.)

---

## 10. النشر السحابي 24/7

| الحقل | القيمة |
|---|---|
| المنصة | Render — Background Worker |
| المنطقة | Frankfurt (EU Central) |
| الأمر | `python -m app.main bot` |
| التخزين | قرص دائم على `/var/data` |
| قاعدة البيانات | `sqlite:////var/data/ics.db` |
| التكلفة | ≈ $7.25/شهر |
| النشر | تلقائي عند كل `git push` |

**الجدولة المدمجة (داخل عملية البوت نفسها):**
- 📊 تقرير يومي: كل يوم **01:15 بتوقيت السعودية**.
- 📈 تقرير أسبوعي: كل **جمعة 01:20** (مع حساب عائد SPY الأسبوعي).

العمل الثقيل يجري في خيط منفصل فلا يعطّل الردود على الأوامر.

---

## 11. الأمان والالتزام بالقيود ✅
- ❌ لا أي كود تداول حقيقي (`real_order/live_trade/margin/short/options/crypto`) — تم التحقق بالبحث.
- ✅ الوضع مقفول على `paper_only` (يُرفض أي وضع آخر عند الإقلاع).
- ✅ الوسيط طويل فقط ومموّل نقدًا — لا يمكن للمحفظة أن تصبح سالبة (لا رافعة).
- ✅ تيليجرام محمي بالصلاحيات: غير المصرّح لهم يصلهم «غير مصرّح لك» فقط.
- ✅ التوكن لم يُرفع للمستودع (`.env` متجاهَل) — فقط `.env.example`.

---

## 12. الحالة الحالية 🟢
- البوت يعمل 24/7 على Render، نسخة واحدة، بلا تعارض.
- الأوامر التفاعلية تعمل بالعربية (`/status`, `/today`, `/portfolio`, `/rules`, `/rejected` …).
- التقارير التلقائية مجدولة وفعّالة.
- قاعدة البيانات دائمة (المحفظة لا تُمسح).

---

## 13. الفجوات المعروفة
- الاستراتيجيات بسيطة/مفسَّرة (غير محسّنة بارامتريًا).
- جودة البيانات تعتمد على yfinance المجاني (يتذبذب أحيانًا).
- «الحدث الاقتصادي الحاد» (مفتاح الإيقاف مستوى 2) علم يدوي — لا تغذية أخبار بعد.
- لا توجد لوحة ويب — التفاعل عبر تيليجرام فقط.

---

## 14. التوصيات المستقبلية
1. **ICS-DOC-003 — حوكمة التداول الحقيقي** (وثيقة منفصلة، خارج هذا الإصدار تمامًا).
2. الترحيل إلى PostgreSQL (طبقة المستودعات جاهزة لذلك بلا تغيير كود).
3. استراتيجيات أغنى + تحسين بارامتري مع تحقّق Walk-forward سليم.
4. تغذية أخبار/اقتصاد كلي لتفعيل علم المستوى 2 تلقائيًا.
5. لوحة ويب بجانب تقارير تيليجرام + إسناد أداء لكل استراتيجية.

---

## 15. الخلاصة
أُنجز المشروع كاملًا من الفكرة إلى الإنتاج: **MVP متكامل ومختبَر (50 اختبار)**،
مُتحقَّق منه بباك تيست حقيقي على 5 سنوات، **معرّب بالكامل**، و**منشور ويعمل 24/7
على السحابة** مع تقارير تلقائية مجدولة — مع الالتزام الصارم بأنه **تداول ورقي
تعليمي فقط**.

---

## 16. تحديث v1.1 — الاستقرار والاستمرارية (Stability & Persistence)

ترقية لا تمسّ منطق التداول/الاستراتيجيات/DQS إطلاقًا — تعديلات في طبقة قاعدة
البيانات والبوت فقط.

| البند | التفصيل |
|---|---|
| **دعم PostgreSQL** | عبر `DATABASE_URL`، مع إبقاء SQLite كخيار محلي افتراضي. تطبيع تلقائي لروابط `postgres://` إلى محرّك psycopg. |
| **init-db آمن** | `create_all` خامل: يعمل على قاعدة فارغة، ولا يكسر/يمسح قاعدة موجودة عند إعادة التشغيل. |
| **توقيتات موحّدة** | تخزين كل الأوقات بـ UTC غير مُوقَّت (naive) لتوافق SQLite و Postgres معًا. |
| **استقرار الاتصال** | `pool_pre_ping` + `pool_recycle` (إسقاط الاتصالات الميتة بدل الانهيار). |
| **أمر `/health`** | يعرض: حالة البوت، المجدول، اتصال القاعدة (اسم اللهجة فقط)، الوضع paper_only، مفتاح الإيقاف، آخر دورة قرار، آخر تقرير يومي، وتطابق **قرارات = تدقيق** — **بدون أي أسرار**. |
| **تقرير حالة/نسخ احتياطي يومي** | نبضة مجدولة مختصرة (13:00 افتراضيًا) تؤكّد الصحة وتحفظ نقطة حالة خفيفة (`last_state_backup`). |

**الاختبارات:** أُضيف 9 اختبارات (الإجمالي **59 — كلها تمر**): اختيار رابط القاعدة،
تطبيع روابط Postgres، عمل SQLite محليًا، خمول init-db، `/health` للمصرّح لهم فقط،
`/health` لا يسرّب أسرارًا، وثبات تطابق القرارات/التدقيق.

**متغيّرات البيئة:** **لا متغيّرات جديدة** — `DATABASE_URL` نفسه صار يقبل روابط
Postgres. (إضافة إعداد واحد في `config.yaml`: `status_report_time_ksa`.)

**الممنوعات محفوظة:** لا تداول حقيقي، لا رافعة/بيع مكشوف/وسيط حقيقي، `/backtest`
لا يعمل داخل البوت، والأمان `paper_only` سليم.

---

*هذا التقرير لأغراض تعليمية وتوثيقية. لا شيء فيه نصيحة مالية.*
