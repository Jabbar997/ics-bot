# Investment Command System (ICS) — MVP requirements
# Python 3.11+

# --- Core data / numerics ---
pandas>=2.0
numpy>=1.24
python-dateutil>=2.8

# --- Persistence ---
SQLAlchemy>=2.0
# PostgreSQL driver (v1.1). SQLite remains the default for local dev; set
# DATABASE_URL to a postgresql:// URL to use Postgres instead.
psycopg[binary]>=3.1

# --- Config ---
pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0

# --- Market data (paper / simulation only) ---
yfinance>=0.2.40

# --- Telegram reporting bot ---
python-telegram-bot>=21.0

# --- Scheduling ---
APScheduler>=3.10

# --- Learning / statistics (ICS-DOC-004 Phase 0) ---
# Spearman rank correlation between DQS components and realised returns.
# Pinned exactly per the ICS-DOC-004 execution rule (no open >= for phase libs).
scipy==1.17.1

# --- Machine learning (ICS-DOC-004 Phase 1) ---
# LightGBM: CPU-only gradient boosting, light enough for the current Render worker.
# optuna: bounded Bayesian tuning, hard cap of 100 trials per monthly cycle.
# All pinned exactly; shadow mode only until the DSR / 3-window gate is passed.
lightgbm==4.7.0
scikit-learn==1.9.0
joblib==1.5.3
optuna==4.9.0

# --- Testing ---
pytest>=7.4

# NOTE: Technical indicators are implemented manually in app/features/indicators.py
# (no hard dependency on pandas-ta). yfinance and python-telegram-bot are imported
# lazily so the deterministic core + test suite run without network access.
