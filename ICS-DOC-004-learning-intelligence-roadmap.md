# ICS-DOC-004 — خارطة تطوير الذكاء والتعلم

**مرجع:** `github.com/Jabbar997/ics-bot` (main، بتاريخ هذه الوثيقة)
**النطاق:** تقوية نتائج ICS إلى أقصى حد ممكن عبر تعلم آلي حقيقي + طبقة ذكاء اصطناعي، مع بقاء النظام paper-only بالكامل.
**لا علاقة لهذه الوثيقة بـICS-DOC-003** (حوكمة التداول الحقيقي) ولا تفتح الطريق لها ضمنياً.

**القاعدة الحاكمة للخطة كلها:** كل مرحلة يجب أن تثبت نفسها إحصائياً ضد الأساس الحالي المُوثّق قبل ما تنتقل التالية إليها.

**الأساس (5 سنوات، walk-forward):**

| المقياس | القيمة |
|---|---|
| العائد الإجمالي | **+21.88%** |
| أقصى تراجع | **−5.42%** |
| Sharpe | **1.01** |
| نسبة الفوز | **38.7%** |
| متوسط DQS | **85.15** |

أي مرحلة جديدة لا تُرقّى لتأثير فعلي على القرار إلا بعد تفوقها على هذا الأساس على بيانات خارج العينة — نفس منطق DQS الحالي بالضبط: **لا تنفيذ بدون دليل**.

---

## المرحلة 0 — حلقة التغذية الراجعة من سجل التدقيق (الأساس، إلزامية أولاً)

**لماذا أولاً:** المراحل 1 و2 بدون هذي المرحلة تصير "تعقيد إضافي بلا طريقة تحقق من نفعه". هذي المرحلة تبني آلية القياس اللي بيحتاجها كل شي بعدها.

1. وسّع نموذج `Decision` (أو أضف جدول `DecisionOutcome` مرتبط 1:1) ليشمل عند إغلاق المركز المقابل: `realized_return`, `holding_period_days`, `mfe` (Max Favorable Excursion), `mae` (Max Adverse Excursion). يُملأ هذا الحقل فقط عند إغلاق الصفقة فعلياً في `paper/broker.py` — لا تقدير مسبق.
2. أنشئ وحدة جديدة `app/learning/feedback_loop.py` تعمل كمهمة أسبوعية داخل `scheduler` الحالي:
   * تسحب كل القرارات المغلقة منذ آخر تشغيل.
   * **بوابة الحد الأدنى:** لا تنفذ أي تعديل قبل توفر ≥ 30 صفقة مغلقة تراكمياً. قبل هذا الحد، سجّل فقط ولا تُعدّل شيئاً.
   * احسب الارتباط (Spearman كافٍ) بين كل مكوّن من مكونات DQS الخمسة وبين `realized_return`.
   * عدّل الأوزان ضمن حدود صارمة: **±5 نقاط مئوية مطلقة** كحد أقصى لكل مكوّن في كل دورة أسبوعية (توضيح صريح: **نقاط، لا نسبة من وزن المكوّن نفسه** — أي `strategy_alignment` عند 25 يتحرك بحد أقصى بين 20 و30 في دورة واحدة، لا 25±1.25). أضِف **أرضية 5 وسقف 40** لكل مكوّن. والمجموع يبقى دائماً = **100** عبر إعادة توزيع صفرية المحصلة.
3. أضف نموذج DB جديد `LearningEvent`: التاريخ، الأوزان قبل/بعد، حجم العينة، معامل الارتباط لكل مكوّن، والسبب النصي — يُسجَّل بنفس صرامة `AuditLog`. **قرار بدون سجل تعلّم = غير صالح.**
4. أضف أمر `/learning` لتيليجرام يعرض آخر تعديل أوزان وسببه.
5. **اختبار إلزامي:** محاكاة 30+ صفقة اصطناعية بنتائج معروفة مسبقاً، والتحقق أن التعديل يتجه للاتجاه الصحيح إحصائياً ولا يتجاوز الحد.

**معيار النجاح للانتقال للمرحلة 1:** تشغيل هذي الحلقة **لدورتين أسبوعيتين على الأقل** بدون كسر أي اختبار قائم، وبدون أن ينخفض **متوسط DQS عن 80**.

---

## المرحلة 1 — طبقة تعلم آلي (تُكمّل القواعد الثابتة، لا تستبدلها فوراً)

1. وحدة جديدة `app/ml/`: تدريب **LightGBM** (خفيف، بلا GPU، مناسب لخادم Render) يتوقع العائد المستقبلي لـN أيام بناءً على نفس الـFeatures الموجودة أصلاً في `feature_engine.py` (لا فيتشرز جديدة في هذي المرحلة).
2. إعادة تدريب **Walk-forward شهرية صارمة**: التدريب فقط على بيانات متاحة حتى تاريخ التدريب، بلا نظرة مستقبلية — نفس القيد المُختبر في `test_backtester_no_lookahead_fills_at_next_open`. خزّن `model_version` + نافذة التدريب مع كل قرار متأثر به.
3. **وضع الظل (Shadow Mode) إلزامي أولاً:** تنبؤات بالتوازي مع DQS لمدة لا تقل عن دورة باك تيست 5 سنوات كاملة + 3 أسابيع Paper حية، في جدول منفصل، **بدون أي تأثير على القرار**. قارن "DQS + ML" مقابل "DQS فقط".
4. لا تُفعَّل مساهمة النموذج إلا بعد إثبات تفوق إحصائي واضح. عند التفعيل: أضِف مكوّناً سادساً `ml_confidence` **بدل استبدال** أي مكوّن قائم، وأعد توزيع الأوزان بقرار موثّق صريح (**ليس تلقائياً** عبر حلقة المرحلة 0 — قرار بنيوي يحتاج اعتماد بشري).

### تعديل — تشديد إحصائي المرحلة 1 (مُلحق لاحقاً)

يُطبَّق قبل بدء المرحلة 1 فعلياً، فوق ما ورد أعلاه:

1. **مكتبة إضافية `optuna`** لضبط بارامترات `lightgbm` (`learning_rate`, `num_leaves`, `max_depth`, `min_child_samples`…) عبر بحث بايزي ضمن **مساحة بحث محدودة مسبقاً في الكود** (لا بحث مفتوح)، وسقف **100 محاولة كحد أقصى** لكل دورة تدريب شهرية. كل محاولة تُسجَّل إلزامياً: رقم المحاولة، البارامترات، نتيجة التحقق.
2. **بوابة نجاح كمّية بدل "تفوق واضح":**
   * احسب **Deflated Sharpe Ratio (DSR)** بدل Sharpe الخام — يأخذ بالحسبان عدد المحاولات الكلي (تدريب + `optuna`) لمنع الاحتفال بتحسّن ظهر بالصدفة.
   * التحقق على **3 نوافذ زمنية غير متداخلة على الأقل**؛ النموذج ناجح فقط إذا تفوّق على الأساس الموثق (Sharpe 1.01، عائد +21.88%، تراجع −5.42%) في **أغلبية النوافذ**، لا نافذة واحدة مختارة.
   * وثّق DSR ونتيجة كل نافذة على حدة في تقرير نهاية المرحلة، بجانب المقاييس الموجودة في `PROJECT_REPORT.md`.
3. `ml_confidence` لا يُفعَّل في القرار الفعلي إلا بعد اجتياز هذا المعيار الكمي كاملاً، فوق شرط وضع الظل.

**السبب:** منع صدفة إحصائية ناتجة عن تعدد محاولات الضبط (كل محاولة `optuna` تجربة يجب احتسابها إحصائياً).

---

## المرحلة 2 — طبقة الذكاء الاصطناعي التوليدي (LLM)

**قاعدة حاكمة تُطبَّق حرفياً:** النموذج اللغوي **لا يتخذ قرار تداول ولا يغيّر `KillSwitchLevel` مباشرة أبداً**. فقط يقترح إشارة + درجة ثقة + سبب نصي، والقرار النهائي لكود حتمي (Risk Manager) أو لمراجعة بشرية.

1. **أتمتة علم "الحدث الاقتصادي الحاد"** (فجوة معروفة — مستوى 2 حالياً علم يدوي):
   * مهمة يومية تجلب عناوين أخبار/بيانات اقتصادية كلية (`feedparser` عبر RSS، خلف واجهة تجريد قابلة للاستبدال).
   * ترسلها لنموذج لغوي بمخرجات **مقيّدة بمخطط ثابت** (Schema-validated JSON فقط: `severe_event: bool`, `confidence: float`, `reasoning: str`) — لا نص حر يُفسَّر يدوياً.
   * عند ثقة منخفضة أو غموض → **تنبيه تيليجرام لمراجعة بشرية** بدل التفعيل التلقائي. التفعيل التلقائي فقط عند ثقة عالية وواضحة، ويُسجَّل كحدث تدقيق كامل (النص الأصلي + نتيجة الاستخراج + إصدار النموذج).
2. **إثراء تفسير القرار في تقارير تيليجرام فقط** (طبقة تجميلية صفرية الخطر): بعد اتخاذ القرار الحتمي بالكامل، استخدم LLM اختيارياً لصياغة شرح أوضح في `/today` و`/weekly` — **لا يؤثر على DQS ولا على القرار**، فقط على وضوح النص.

---

## المرحلة 3 — تحسين عام (بدون AI جديد)

1. تحسين بارامتري للاستراتيجيات الأربع عبر Walk-forward validation صارم (بحث محدود النطاق، لا Grid Search مفتوح).
2. مصدر بيانات احتياطي بديل عن yfinance عند التذبذب.
3. توسعة قائمة المتابعة تدريجياً — رموز أكثر = تراكم أسرع لعينة صفقات كافية لحلقة المرحلة 0.
4. لوحة ويب — أولوية أخيرة.

---

## الترتيب الإلزامي والقياس

**لا تنفيذ متوازٍ.** الترتيب: **0 → 1 → 2 → 3**، وكل مرحلة تحتاج معيار نجاحها قبل الانتقال.

حدّث `PROJECT_REPORT.md` بعد كل مرحلة بنتائج القياس الفعلية (العائد، أقصى تراجع، Sharpe، نسبة الفوز، متوسط DQS) — **لا تكتب "تم" بدون رقم يقارن بالأساس**.

النظام يبقى `mode: paper_only` مقفولاً طوال كل هذي المراحل **بدون استثناء**. أي انتقال لتداول حقيقي خارج نطاق هذي الوثيقة بالكامل ويحتاج ICS-DOC-003 منفصلة.
